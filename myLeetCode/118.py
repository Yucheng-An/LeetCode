def generate(numRows):
    res = []
    for i in range(numRows):
        subArray = []
        for j in range(0,i+1):
            if j != 
        res.append(subArray)
    return res



t = generate(5)
print(t)