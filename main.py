def example(num: int):
    sizeMatrix = num * 2-1
    matrix = [[num]*sizeMatrix] * sizeMatrix
    for i in range(0,len(matrix)):
        for j in range(0 , len(matrix)):

    print(matrix)




example(3)