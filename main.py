def example(num: int):
    sizeMatrix = num * 2-1
    matrix = [[num]*sizeMatrix] * sizeMatrix
    for i in range(sizeMatrix):
        for j in range(sizeMatrix):


def 

    print(matrix)




example(3)