def example(num: int):
    sizeMatrix = num * 2 - 1
    matrix = [[num] * sizeMatrix] * sizeMatrix
    print(matrix)
    # return re(matrix, num, 0, sizeMatrix, 1)


def re(matrix, num, small, ):
    if needCall == num:
        matrix[start][end] = 1



res = example(3)
print(res)
